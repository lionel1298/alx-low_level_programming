#include "main.h"
/**
 * mul - multiply
 * @a: first integer operand
 * @b: second integer operand
 * Description: a function that multiplies two integers.
 * Return: product of a and b always.
 */
int mul(int a, int b)
{
	return (a * b);
}
